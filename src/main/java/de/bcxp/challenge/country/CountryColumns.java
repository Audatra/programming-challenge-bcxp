package de.bcxp.challenge.country;

/**
 * The names used for the columns of the CSV file containing the countries
 */
public enum CountryColumns{
        Name,
        Capital,
        Accession,
        Population,
        Area,
        GDP,
        HDI,
        MEPs
}
